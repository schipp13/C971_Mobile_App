<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:C971_MobileApp"
             x:Class="App1.AssessmentsPage">

    <ContentPage.BindingContext>
        <local:CourseViewModel/>
    </ContentPage.BindingContext>

    <StackLayout>
        <Label x:Name="Course Name"
               Text="{Binding Course.courseName}"
               FontSize="Large"
        />
        <BoxView HorizontalOptions="FillAndExpand" HeightRequest="1" Color="Black"/>

        <ListView x:Name="AssessmentItems">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <FlexLayout>
                            <Label x:Name="AssessmentType"
                                   Text="{Binding Test.AssessmentType}"
                                   FontSize="Medium"
                            />
                            <Label x:Name="AssessmentName"
                                   Text="{Binding Test.AssessmentName}"
                                   FontSize="Medium"
                            />
                            <Label x:Name="AssessmentDate"
                                   Text="{Binding Test.AssessmentDate}"
                                   FontSize="Medium"
                            />
                        </FlexLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>
</ContentPage>